# Canonical host: send www -> apex
https://www.willcarpenter.me/*   https://willcarpenter.me/:splat   301

# Example shortlinks (edit/remove)
# /resume    /will_carpenter_resume.pdf   302
# /about     /index.html#about            302

# (If this were an SPA, you’d use the line below instead of the default 404 behavior)
# /*    /index.html    200
